 security/roles_and_permissions.sql-- Create least-privilege roles and grant appropriate permissions.CREATE ROLE app_read NOINHERIT;CREATE ROLE app_write NOINHERIT;GRANT CONNECT ON DATABASE bootcamp TO app_read;GRANT CONNECT ON DATABASE bootcamp TO app_write;GRANT USAGE ON SCHEMA public TO app_read;GRANT USAGE ON SCHEMA public TO app_write;GRANT SELECT ON ALL TABLES IN SCHEMA public TO app_read;GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_write;ALTER DEFAULT PRIVILEGES IN SCHEMA publicGRANT SELECT ON TABLES TO app_read;ALTER DEFAULT PRIVILEGES IN SCHEMA publicGRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO app_write;
Collapse file‎security/security_notes.md‎Copy file name to clipboard+16Lines changed: 16 additions & 0 deletionsDisplay the source diffDisplay the rich diffOriginal file line numberOriginal file lineDiff line numberDiff line change@@ -0,0 +1,16 @@# Security Notes## Least privilegeLeast privilege means granting users and applications only the permissions they need to perform their tasks and no more. This reduces the risk of accidental or malicious data changes and limits the impact of compromised credentials.## Why applications should not use superuser accountsApplications using superuser accounts have full control over the database, including schema changes and system-level operations. If an application account is compromised, an attacker could damage data, change schema, or access sensitive information. Least-privilege accounts contain the blast radius.## app_read vs app_write- `app_read`: granted only `CONNECT`, schema `USAGE`, and `SELECT` on tables.- `app_write`: granted `CONNECT`, schema `USAGE`, and full DML privileges (`SELECT`, `INSERT`, `UPDATE`, `DELETE`) on tables.This separation ensures that read-only application workloads and write-capable workloads are handled by different roles.
Collapse file‎security/user_creation.sql‎Copy file name to clipboard+7Lines changed: 7 additions & 0 deletionsOriginal file line numberOriginal file lineDiff line numberDiff line change@@ -0,0 +1,7 @@-- security/user_creation.sql-- Create an application user with write privileges via the app_write role.CREATE USER api    LOGIN    PASSWORD 'strong-secret'    IN ROLE app_write;
